<catel:Window x:Class="Orc.AutomaticSupport.Views.RequestSupportView"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:catel="http://catel.codeplex.com" Width="300"
              WindowStartupLocation="CenterScreen"  SizeToContent="Height"
              ResizeMode="NoResize">

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Grid.Row="0">
            <TextBlock TextWrapping="Wrap">
                The software is currently downloading the automatic support application. Once it is ready, it will automatically run the support
                application in order to analyze the installed software and fix any problems it might find.
            </TextBlock>
        </Label>

        <ProgressBar Grid.Row="1" Minimum="0" Maximum="100" Value="{Binding Progress}" Height="20" />

        <Label Grid.Row="2">
            <TextBlock Text="{Binding RemainingTime, StringFormat=Remaining time: {0}}" />
        </Label>
    </Grid>
</catel:Window>
